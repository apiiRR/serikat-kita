project_id = "vxezdmmtjyegjwuceiny"
# Public request endpoint validates the payload server-side. The review endpoint
# explicitly verifies the bearer JWT with auth.getUser and checks the admin role.
[functions.request-document-download]
verify_jwt = false

[functions.review-document-request]
verify_jwt = false
