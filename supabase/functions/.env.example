# Copy to supabase/.env.smtp.local (gitignored). Never use VITE_ for these secrets.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
# Optional: defaults to SMTP_USER. Use the mailbox or an authorized Workspace alias.
SMTP_FROM=
